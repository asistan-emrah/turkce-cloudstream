// use an integer for version numbers
version = 1


android {
    defaultConfig {
        // Sürüm adını ve kodunu buradan ayarlayabilirsiniz
        versionName = "1.0.0"
        versionCode = 1
    }
}

dependencies {
}
